# SES Email Service 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env

# ==================================================
# 테스트용 이메일 주소
# ==================================================

# 발신자 이메일 (SES에서 인증된 이메일 주소)
TEST_FROM_EMAIL=sender@yourdomain.com

# 테스트 수신자 이메일
TEST_TO_EMAIL=recipient@example.com

# ==================================================
# AWS 설정 (선택사항)
# ==================================================

# AWS Profile (기본값: default)
# AWS_PROFILE=default

# AWS Region (기본값: us-east-1)
# AWS_REGION=us-east-1

# SES Configuration Set Name (기본값: my-first-configuration-set)
# SES_CONFIG_SET_NAME=my-first-configuration-set

# ==================================================
# SQS 큐 URL (로컬 테스트용)
# ==================================================

# SQS Queue URL (sam deploy 후 Outputs에서 확인)
# SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/dev-email-queue
